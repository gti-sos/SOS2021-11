name: CI

on:
  push:
      branches: [ main ]

jobs: 
  integrationTests:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install &
      - run: npx newman tests/anxiety_stats_integration.json